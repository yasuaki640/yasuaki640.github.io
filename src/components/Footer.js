import React from 'react';
import Button from "react-bootstrap";

class Footer extends React.Component {
    render() {
        return (
            <div>this is a header we creating</div>
        );
    }
}

export default Footer;